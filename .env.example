# Copy this file to .env and fill in one of the two auth methods below.
# .env is gitignored - never commit actual secrets.

# Option A (recommended): run `claude setup-token` on the host to get
# a token, then paste it here.
CLAUDE_CODE_OAUTH_TOKEN=

# Option B: use an API key instead.
ANTHROPIC_API_KEY=
